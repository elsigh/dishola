{
	"name": "@dishola/api",
	"private": true,
	"scripts": {
		"build": "nitro build",
		"dev": "PORT=3001 nitro dev",
		"prepare": "nitro prepare",
		"preview": "node .output/server/index.mjs",
		"lint": "echo 'No linting configured'",
		"clean": "rm -rf .output"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.0.0",
		"nitropack": "latest"
	},
	"dependencies": {
		"@ai-sdk/gateway": "alpha",
		"@ai-sdk/provider": "latest",
		"ai": "alpha"
	}
}
