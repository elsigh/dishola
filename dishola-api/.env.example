# AI Service Configuration (using Vercel AI SDK 5)
# Supported providers: gateway (default), openai, anthropic, google
AI_PROVIDER=gateway

# ============ VERCEL AI GATEWAY (Recommended) ============
# Uses Vercel AI Gateway for access to 100+ models without API keys
# Handles authentication, rate limiting, and usage tracking
# Available models: openai/gpt-4, anthropic/claude-3-sonnet, google/gemini-pro, etc.
AI_GATEWAY_MODEL=openai/gpt-3.5-turbo

# For local development, run: `vc env pull` to get authentication token
# For production: Deploy to Vercel and authentication is automatic

# ============ DIRECT PROVIDER ACCESS (Fallback) ============
# Only needed if not using gateway (AI_PROVIDER != 'gateway')

# API Keys - Set the one for your chosen provider
AI_API_KEY=your_api_key_here

# Provider-specific environment variables (optional - can use AI_API_KEY instead)
# OPENAI_API_KEY=your_openai_key_here
# ANTHROPIC_API_KEY=your_anthropic_key_here  
# GOOGLE_GENERATIVE_AI_API_KEY=your_google_key_here

# Get API keys:
# OpenAI: https://platform.openai.com/api-keys
# Anthropic: https://console.anthropic.com/
# Google AI: https://makersuite.google.com/app/apikey